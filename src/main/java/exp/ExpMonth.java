package exp;

import options.Options;
import serverObjects.BASE_CLIENT_OBJECT;
import tws.TwsContractsEnum;

public class ExpMonth extends Exp {

    public ExpMonth( BASE_CLIENT_OBJECT client ) {
        super( client );
    }
}
