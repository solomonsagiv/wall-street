package exp;

import options.Options;
import serverObjects.BASE_CLIENT_OBJECT;
import tws.TwsContractsEnum;

public class ExpWeek extends Exp {

    // Constructor
    public ExpWeek( BASE_CLIENT_OBJECT client ) {
        super( client );
    }
}
