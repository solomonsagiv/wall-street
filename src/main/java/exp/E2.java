package exp;

import options.Options;
import serverObjects.BASE_CLIENT_OBJECT;

public class E2 extends Exp {

    public E2(BASE_CLIENT_OBJECT client, Options options ) {
        super(client, options, indexOptionsCalc);
    }
}
