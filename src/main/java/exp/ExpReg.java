package exp;

import serverObjects.BASE_CLIENT_OBJECT;
import tws.TwsContractsEnum;

public class ExpReg extends Exp {

    public ExpReg( BASE_CLIENT_OBJECT client, String expName) {
        super(client, expName);
    }
}

